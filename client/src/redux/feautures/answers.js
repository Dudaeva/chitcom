const initialState = {
    settingTheBestAnswer: false
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

const addAnswer = () => async (dispatch) => {
    dispatch({})
}

export default reducer;